var config = {
  client: 'pg',
  connection: {
    host     : '127.0.0.1',
    user     : 'ricaldi',
    password : 'password',
    database : 'ricaldi',
    timezone : "UTC"
  }
}

module.exports = config;
